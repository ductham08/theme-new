import axios from "axios";

export const sendDataApi = async (data:object) => {
    console.log(data);

    // const response = await axios.post(`${import.meta.env.VITE_URL_SERVER}/api/resgister`, values);
    // return response;
}